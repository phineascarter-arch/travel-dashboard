{
  "scripts": {
    "build": "esbuild app.js --bundle --outfile=dist/app.bundle.js --format=iife",
    "watch": "esbuild app.js --bundle --outfile=dist/app.bundle.js --format=iife --watch"
  },
  "dependencies": {
    "motion": "^12.43.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
